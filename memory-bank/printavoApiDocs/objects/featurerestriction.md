# FeatureRestriction

Feature restriction details

## Fields

### currentUsage (`Int!`)
Current amount used

### limit (`Int!`)
The maximum allowed usage

### resetsAt (`ISO8601DateTime`)
When the usage resets
